{
  "name": "geo-lms-simulator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "sim": "npx --yes tsx src/cli/run-sim.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint:contracts": "node scripts/validate-contracts.mjs",
    "ci": "npm run lint:contracts && npm run test"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "jsdom": "^28.1.0",
    "typescript": "^5.8.2",
    "vitest": "^3.0.8"
  }
}
